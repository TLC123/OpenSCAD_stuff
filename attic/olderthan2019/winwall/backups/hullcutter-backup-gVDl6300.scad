


cut(1)
import("bunny.stl", convexity=3);

module cut(s){
    r=s*0.9;
   if(r>0.1){ cut(r)
    intersection()
{
color("Red")   children(0);

scale(r)hull(){
    children(0);
}}}else{children(0);}
    
    
    
    }